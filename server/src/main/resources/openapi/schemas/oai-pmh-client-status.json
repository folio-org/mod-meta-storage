{
  "description": "OAI PMH client status",
  "type": "object",
  "properties": {
    "status": {
      "type" : "string",
      "enum" : [ "idle", "running"],
      "description" : "OAI PMH job status"
    },
    "error" : {
      "type" : "string",
      "description" : "Error message for last error"
    },
    "config" : {
      "description" : "configuration",
      "type" : "object",
      "$ref" : "oai-pmh-client.json"
    }
  },
  "additionalProperties": false,
  "required": [
    "status", "config"
  ]
}
